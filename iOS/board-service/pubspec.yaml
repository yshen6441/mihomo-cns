name: board_service
description: Board (v2board/xboard/sspanel) login service for Clash Mi (reimplemented).
version: 0.12.15
homepage: https://github.com/clashmi
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  crypto: ^3.0.0
  pointycastle: ^3.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
